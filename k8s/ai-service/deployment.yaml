apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: distributed-health
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service
          image: amzalfoumi/ai-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3008
          envFrom:
            - secretRef:
                name: ai-service-secret
                optional: true
            - configMapRef:
                name: ai-service-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
