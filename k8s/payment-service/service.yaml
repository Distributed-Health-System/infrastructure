apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: distributed-health
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
