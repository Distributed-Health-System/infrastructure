apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: distributed-health
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: patient-service-secret
    envs:
      - .env.secret
