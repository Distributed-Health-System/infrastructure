apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: distributed-health
spec:
  type: ClusterIP
  selector:
    app: patient-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
