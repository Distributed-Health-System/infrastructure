apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: distributed-health
spec:
  type: ClusterIP
  selector:
    app: appointment-service
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
