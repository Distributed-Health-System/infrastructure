apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: distributed-health
spec:
  type: ClusterIP
  selector:
    app: doctor-service
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
